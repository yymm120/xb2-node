```js
/**
 * 理解异步
 */
// js中只有一个线程处理任务,只能处理一个任务，这个任务就是一个执行栈。
// js从任务队列头部消费任务，并将异步任务交给其他线程处理(非js线程，如浏览器)，并告知回调函数。
// 非js线程处理完‘异步’任务后，将回调函数压入任务队列的头部，任务队列是先进先出的数据结构。
// js线程从头部读取到该任务，无阻塞的执行。
/**
 * 总结异步：
 * js 线程监听任务队列，无阻塞的消费消息。而异步体现在js将某些任务交给了其他线程处理，自己则从
 */
```